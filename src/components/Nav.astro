---
import { cn } from '@utils/cn'

const pathname = new URL(Astro.request.url).pathname
const currentPath = pathname.slice(1)
---

<nav class="mx-auto h-16 flex-shrink-0 px-4 sm:mx-0 md:px-6">
  <ul class="mx-auto flex h-full max-w-screen-md items-center">
    <li>
      <a
        class={cn('px-4 py-2 font-medium hover:underline', {
          'rounded bg-neutral-700 hover:bg-neutral-500': !currentPath,
        })}
        href="/">Home</a
      >
    </li>

    <li>
      <a
        class={cn('px-4 py-2 font-medium hover:underline', {
          'rounded bg-neutral-700 hover:bg-neutral-500':
            currentPath.includes('blog'),
        })}
        href="/blog">Blog</a
      >
    </li>

    <li>
      <a
        class={cn('px-4 py-2 font-medium hover:underline', {
          'rounded bg-neutral-700 hover:bg-neutral-500':
            currentPath.includes('projects'),
        })}
        href="projects"
      >
        Projects</a
      >
    </li>
  </ul>
</nav>
